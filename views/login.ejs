<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>PennyWise</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Kaisei+Opti&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Sedan+SC&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/style/style.css">
</head>

<body class = "bg-[#6E0924]">
    <div class = 'flex flex-col items-center h-dvh w-dvw'>
        <div id = 'spacer' class = "bg-inherit h-[10%]"></div>
        <img src="/images/navbarIcons/logo.png" class="blur-[0.5px] h-2/5 p-4" id="logo" alt="logo">
        <div class = 'flex flex-col gap-y-6 items-center justify-center bg-[#D3D0CB] rounded-lg h-2/6 w-3/5'>
            <form method="POST" class="flex flex-col items-center justify-center gap-y-4 text-center">
                <input type="text" name="email" class="mx-auto bg-slate-50 w-1/2 min-w-52 py-4 rounded-full font-semibold shadow-md px-4" placeholder="<%= error === 'invalid_email' ? 'Invalid email' : 'Enter your email' %>">
                <input type="password" name="password" class="mx-auto bg-slate-50 w-1/2 min-w-52 py-4 rounded-full font-semibold shadow-md px-4" placeholder="<%= error === 'incorrect_password' ? 'Incorrect password' : 'Enter your password' %>">
                <button type = "submit" id = "login" class="bg-slate-50 mx-auto text-black font-medium text-lg p-2 w-3/4 rounded-full shadow-md">Login</button>
            </form>
        </div>
    </div>
</body>
</html>